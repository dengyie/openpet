# OpenPet Development Summary

> Last updated: 2026-06-28
> Branch: `main`
> Current release track: `v1.0.1-rc.3`

This is the short engineering summary for the current repository state. For long phase history, read `docs/phases/` and `docs/reviews/`. For support claims and documentation rules, read `docs/project-documentation-design.md`.
For the current code-aligned TODO map, read `docs/openpet-current-todo-architecture.md`. For the latest phase execution design, read `docs/productization-v1.1-todo-design.md`. For current platform status, read `docs/project-status-review.md`.

## Current State

OpenPet is now a desktop pet platform with:

- an Electron pet window,
- optional grounded pet movement and host-owned home-anchor roaming controls exposed in Control Center,
- a React + Vite Control Center,
- pet pack import and bundled pet packs,
- AI chat with secret storage in the main process, active/draft provider settings, separate save/test connection checks, structured provider diagnostics, pet-pack persona/history/memory, desktop chat, a collapsible Control Center AI settings page with chat/image Provider sections expanded by default while secondary AI tools stay collapsed, and host-owned unified image Provider settings for Creator Studio,
- developer-first local extension docs with explicit `entries.setup` execution, language-neutral explicit `entries.commands` process execution, explicit command result feedback, short-lived command bridge access, creator-tools action reads / validation / bounded writes, active installed user pack metadata workflows, package-local frame inspection/import, user-approved picker frame inspection/import for declaration-only commands, reviewed action trigger proposal acceptance, explicit dashboard opening, explicit service start/stop controls, explicit loopback service health checks, host-managed periodic service health policy for running services, broader host-owned process-tree fallback cleanup for explicit local-process stops, current-host plugin cleanup evidence collection, cleanup evidence helper generation, cleanup evidence runner archives, packaged-app plugin cleanup evidence runner archives, cleanup evidence archive manifests, structured plugin cleanup readiness reports with validation-first updates, structured maintainer approval rehearsal artifacts for submission bundles, existing-plugin real-world submission rehearsal evidence, remote-source submission rehearsal evidence, community-source submission evidence tooling, community-source candidate discovery reporting, community-source invitation kits, community-source candidate intake reporting, and compatible-intake-to-submission bridge tooling,
- loopback-only local HTTP / MCP endpoints,
- and a TypeScript migration baseline covering shared IPC, Control Center view contracts, the Control Center API facade, Control Center hook state boundaries, Control Center pane prop surfaces, main-process Control Center adapters for service/catalog/plugin/pet pack/About/update/actions payloads, plugin submission evidence contracts, community-source invitation evidence contracts, plugin cleanup archive/runner contracts, packaged plugin cleanup evidence contracts, AI provider smoke report contracts, Creator Studio provider smoke report contracts, macOS release evidence summary/archive contracts, Windows smoke report/evidence summary/archive contracts, desktop picker smoke report contracts, desktop picker evidence summary/archive contracts, packaged runtime smoke report/evidence contracts, full release evidence archive / signed closure report contracts, and representative product payload fixtures.

## Current Capability Summary

- Pet packs: Codex pet directory/zip import, bundled `doro`, `duodong`, `chispa`, export, provenance, and version-conflict review.
- Pet behavior: scale, walk speed/duration, bubble timing, auto-start, optional grounded movement, optional home-anchor roaming, drag-to-rehome when home is enabled, and display-change normalization for saved home anchors.
- AI: OpenAI-compatible chat, main-process secret storage, active/draft provider settings, separate save/test provider actions, structured sanitized connection diagnostics, chat/image provider presets with conservative claim boundaries, optional `/models` discovery, provider-aware chat/image compatibility hints, a model-settings-first AI pane that keeps `聊天 Provider` and `图片 Provider` open while memory/persona/behavior/chat sections stay collapsed until expanded, explicit host-owned trust/save-test copy, `npm run smoke:ai-provider` for sanitized real-gateway smoke validation, archived real AI provider smoke evidence under `docs/release-evidence/ai-provider-smoke/2026-06-28T11-08-10Z-openpet-gateway/` proving `/models` exposed `gpt-5.5` and `gpt-image-2` while `gpt-5.5` passed chat smoke and image generation remained intentionally skipped, `npm run smoke:creator-studio-provider` for sanitized host-side Creator Studio prompt -> image generation -> action-frame QA smoke validation, archived real Creator Studio provider smoke evidence under `docs/release-evidence/creator-studio-provider-smoke/2026-06-28T14-06-27-403Z/` for the current `gpt-image-2` gateway path, AI Talk persona/history/memory, desktop chat, behavior decisions, replay, redacted diagnostics export, clear-history controls, trace export filters, and host-owned unified image Provider settings for Creator Studio. Common OpenAI/OpenRouter/Together/LM Studio/vLLM/local presets are endpoint templates that still require save plus connection test or health check; only the OpenPet 8317 gateway preset is tied to the archived OpenPet gateway smoke evidence. The archived AI provider smoke does not prove image generation output quality or production asset readiness.
- Extensions: current legacy SDK examples, validation, submission bundles, scaffold author rehearsal, existing-plugin real-world submission rehearsal, remote-source submission rehearsal, community-source discovery reporting, community-source invitation kits, community-source submission evidence, community-source candidate intake reporting, compatible-intake-to-submission bridge tooling, maintainer approval rehearsal, plugin cleanup evidence collection/reporting/updating/helper generation/runner archives/archive manifests, packaged-app plugin cleanup evidence reporting and runner archives, developer-first local extension docs, normalized `entries` declarations including explicit `entries.setup` execution, `entries.commands` support through the existing JavaScript compatibility runner and explicit short-lived process execution for declaration-only local extensions, command result feedback in Control Center, short-lived command bridge access for `pet.say` / `pet.action` / `pet.event` / read-only context, host-owned `OPENPET_DATA_DIR` / `OPENPET_CACHE_DIR` / `OPENPET_LOG_DIR` injection for declaration-only command runs, creator-tools action reads / validation / bounded writes, active installed user pack metadata workflows, package-local frame inspection/import, user-approved picker frame inspection/import through the same short-lived bridge, reviewed action trigger proposal acceptance for `click`, `manual`, and `unbound`, active host-owned durable trigger rules for `random`, `state`, and `event`, a persisted Trigger Proposal Inbox review path in Control Center Actions, Control Center declaration visibility, explicit HTTP/HTTPS dashboard opening, explicit `entries.services` start/stop with runtime status and logs, manual loopback-only health checks, opt-in host-managed periodic health checks for running services, best-effort process-group cleanup on service stop, exit-confirmed setup/command/service stop semantics, bounded host-side force stop for stubborn services, host-owned process-tree fallback cleanup across explicit service/setup/declaration-command stop paths, separate maintainer approval artifacts beside ready-for-review submission bundles, and route/permission docs backed by targeted regression tests, including `trigger-proposals:write` for Creator Studio review handoff and `model:image-generate` for host-managed settings, health checks, and bounded image generation. Command, setup, and service processes are spawned without shell expansion; setup and command entries never run during install or enable, services do not auto-start, approval remains a human review decision, plugin-managed provider credentials are unsupported on the host-managed generation surface, raw filesystem grants are not exposed to plugin commands, and packaged cleanup evidence still proves only the observed packaged run rather than universal process-tree cleanup guarantees.
- Release evidence: packaged runtime smoke runner, stricter runtime/picker evidence-link validation, desktop picker evidence summary/archive manifest tooling, Windows smoke evidence summary/archive manifest tooling, macOS codesign/notarization/Gatekeeper evidence capture with GitHub Actions artifact upload and permanent artifact archive handoff, release archive manifest tooling that now requires reviewed Windows smoke and desktop picker archive manifests, and signed release closure reporting without unsupported readiness claims.
- TypeScript: shared IPC/view contracts, typed Control Center API facade, typed Control Center hook state boundaries, typed Control Center pane prop surfaces, `@ts-check` main-process Control Center adapters for service/catalog/plugin/pet pack/About/update/actions payloads, plugin extension entry contracts, plugin submission evidence contracts, community-source invitation evidence contracts, plugin cleanup archive/runner contracts, packaged plugin cleanup evidence contracts, AI provider smoke report contracts, Creator Studio provider smoke report contracts, macOS release evidence summary/archive contracts, Windows smoke report/evidence summary/archive contracts, desktop picker smoke report contracts, desktop picker evidence summary/archive contracts, packaged runtime smoke report/evidence contracts, full release evidence archive / signed closure report contracts, and representative payload fixtures in `npm run typecheck`.

## Validation Baseline

```bash
npm test                     # full Node regression suite
npm run test:control-center  # Playwright UI regression baseline
npm run typecheck            # TypeScript no-emit checks
npm run check:syntax         # Node syntax + typecheck + Control Center build
```

`npm run test:control-center` now starts its own isolated Control Center dev server on `http://127.0.0.1:4173` by default, so it does not silently reuse another local Vite app already bound to `5173`. Set `OPENPET_CONTROL_CENTER_PORT` to override the port, and set `OPENPET_CONTROL_CENTER_REUSE_SERVER=1` only when you intentionally want Playwright to reuse an already-running OpenPet Control Center server on that same port.

Targeted checks for the current AI/settings/action-trigger slice:

```bash
node --test tests/services/ai-service.test.js tests/services/action-service.test.js tests/main/control-center-adapters.test.js tests/main/ipc-plugin-install.test.js
npm run test:control-center -- --grep "AI config|save and test uses the current AI drafts|applies an action trigger proposal|loads the app shell"
```

## Open Work

- Windows is still not release-ready until real signed installer evidence and smoke reports are archived.
- AI provider settings UX is now implemented for chat and image provider configuration: active config and drafts are separated, save and test or health actions are visible inside the provider sections, the AI pane now foregrounds the chat/image Provider sections and keeps secondary AI tools collapsed by default, save does not depend on test success, connection diagnostics are structured and sanitized, optional `/models` discovery and provider-aware compatibility hints are surfaced in the Control Center UI, host-owned trust/save-test guidance is visible in the first-screen Provider cards, and credentialed Base URLs are not exposed in renderer views. Future AI work should preserve that boundary rather than reintroducing renderer-owned secrets.
- Host image-generation settings now exist for Creator Studio: Control Center and the host runtime both use one OpenAI-compatible image Provider contract with `Base URL`, `Model`, timeout/concurrency, and main-process `API Key` storage through `SecretService`; local and hosted providers are selected by changing the Provider URL rather than by exposing separate `local` / `cloud` modes. The Control Center image Provider section now includes setup guidance, OpenAI-compatible presets, the explicit OpenPet `127.0.0.1:8317/v1` gateway preset, editable timeout/concurrency fields, optional model discovery, provider-aware compatibility hints, safe generation usage summaries when provider metadata exists, the sanitized `npm run smoke:ai-provider` entrypoint for real gateway verification, and the sanitized `npm run smoke:creator-studio-provider` entrypoint for host-side Creator Studio provider-path verification. A successful real Creator Studio provider smoke archive now lives under `docs/release-evidence/creator-studio-provider-smoke/2026-06-28T14-06-27-403Z/`, where the saved `gpt-image-2` path completed prompt build, provider generation with requested `512x512` constraints, and 16-frame QA under a temporary `420000ms` timeout override. Older `fixture` / `cloud` / `local` settings are read as legacy input and normalized into the unified Provider view, while new saves persist only the unified shape. The remaining product work is broader provider catalog coverage plus human review of generated asset quality.
- Creator Studio's dashboard-first wizard is now landed for visible prompt snapshot, task preview, pending question, confirmation, generation, QA review, retry/recover, workflow guidance, prompt provenance, action-lane guidance, and structured host-owned import handoff that points approved action/pet runs to the correct Control Center plugin command without exposing command-scoped bridge credentials. Provider-backed full-pet runs now also build a real generated atlas, write `source-image-validation.json` and `atlas-validation.json`, and block `Import Approved Pet` until QA passes. Imported follow-up routing is now also shared through `workflowGuidance.import.followUp`, so imported review guidance no longer drifts from `nextStep`, `actionLane`, or imported result cards. The remaining product work is broader UX polish and future partial-regeneration depth, not the basic wizard closure.
- Creator Studio imported follow-up routing is now explicit in the landed workflow: imported action success sends review follow-up to `Actions -> Trigger Proposal Inbox`, imported action handoff failures send follow-up to `Control Center -> Plugins`, and imported pet follow-up remains `OpenPet` via `Import Approved Pet`.
- Creator Studio imported review surfaces are now phase-aware as well: after a run is `imported`, imported result guidance stays visible while pre-import QA blocked messaging, repair controls, and retry-generation cues no longer remain in the review panels.
- Action trigger proposal acceptance is implemented for reviewed proposals: `click` can update `clickAction`, `manual` and `unbound` are acknowledged without mutating bindings, and `random`, `state`, and `event` proposals create active host-owned durable trigger rules. Creator Studio action imports now submit their trigger proposal into the Actions `Trigger Proposal Inbox` for host review instead of mutating trigger rules directly from the plugin.
- Packaged native picker and signed release archive evidence still need real archived runs; automated macOS packaged runtime evidence is archived under `docs/release-evidence/packaged-runtime/2026-06-16T14-52-13-074Z-darwin-arm64/`, where the unsigned packaged app launch, transparent rendering, built-in pack switching, and stable-state restoration passed, while `plugin-picker-evidence-linked` and `pet-picker-evidence-linked` remain pending and `invalid-package-feedback` remains blocked until a reviewed desktop picker smoke report is linked. Phase 65 requires packaged runtime readiness to link the paired desktop picker report before archive readiness can be claimed, Phase 66 adds desktop picker evidence summary/archive manifest tooling for the reviewed picker archive, Phase 67 requires the release archive and signed closure flow to consume that picker archive manifest, Phase 77 adds a repeatable macOS evidence capture command for the canonical codesign/notarization/Gatekeeper files, Phase 78 uploads those macOS evidence files as a dedicated release workflow artifact, Phase 79 copies downloaded workflow evidence into a permanent archive with provenance and hashes, and Phase 81 requires the reviewed Windows smoke archive manifest in the release archive and signed closure gate.
- Signed release closure evidence is archived under `docs/release-evidence/signed-release-closure/2026-06-16T15-00-00Z/` and currently records official desktop, macOS, and Windows claims as `not-ready`; missing signed macOS evidence, missing desktop picker evidence, unsigned packaged runtime evidence, and missing signed Windows smoke evidence remain release blockers.
- Extension author rehearsal evidence is archived under `docs/release-evidence/plugin-author-rehearsal/2026-06-16T16-00-00Z/` with minimal, network, storage, and AI-assisted legacy SDK scaffolds, a validated submission bundle, and a separate maintainer approval rehearsal record.
- Existing-plugin real-world submission rehearsal evidence is archived under `docs/release-evidence/plugin-real-world-submission-rehearsal/2026-06-17T15-14-15Z/` using `examples/plugins/weather-status` with package, submission bundle, and maintainer approval artifacts.
- Remote-source submission rehearsal evidence is archived under `docs/release-evidence/plugin-remote-source-submission-rehearsal/2026-06-18T00-30-00Z/` using `https://codeload.github.com/dengyie/OpenPet/zip/refs/heads/main` with archive URL, final URL, archive SHA-256, archive size, selected plugin path, extracted file hashes, package, submission bundle, and maintainer approval artifacts.
- Phase 99 is now complete: maintainers can wrap a remote-source rehearsal with community source URL, submitter label, source relationship, and independence notes, while local fixture coverage keeps the evidence command deterministic and live independent community provenance remains an explicit next-step evidence gap.
- Phase 100 is now complete: maintainers can now archive a community-source candidate intake report before Phase 99, producing either `ready-for-community-evidence` for compatible `plugin.json` packages or `incompatible-package-model` for neighboring public repositories that should not be overstated as OpenPet plugin submissions.
- Phase 101 is now complete: shared TypeScript contracts now cover submission bundles, maintainer approval records, existing-plugin rehearsal summaries, remote-source rehearsal summaries, and community-source evidence summaries so the plugin submission evidence chain type-checks against representative fixtures without changing readiness or trust rules.
- Phase 102 is now complete: a real public `alvinunreal/openpets` candidate-source intake archive exists under `docs/release-evidence/plugin-community-source-intake-report/2026-06-18T23-30-00Z-openpets-official/`; it is intentionally classified as `incompatible-package-model` / `plugin-json-missing`, so it does not enter Phase 99.
- Phase 103 is now complete: compatible Phase 100 intake summaries can be routed into Phase 99 through `npm run create-plugin-community-source-evidence-from-intake`; incompatible Phase 102-style archives are rejected and remain intake evidence only.
- Phase 104 is now complete: community-source discovery can be archived before Phase 100 through `npm run create-plugin-community-source-discovery-report`; the current public search archive is `compatible-source-not-found`, so no compatible third-party `plugin.json` package is claimed.
- Phase 105 is now complete: maintainers can archive a compatible-source invitation kit through `npm run create-plugin-community-source-invitation-kit` or the shorter `npm run create-plugin-community-source-invitation` alias; the invitation proves draft outreach readiness only and still does not prove outreach was sent, accepted, or a compatible third-party source exists.
- Phase 106 is now complete: Phase 105 invitation-kit summaries now have shared TypeScript contracts and representative fixtures, while still representing draft outreach only.
- The extension ecosystem now has twenty-seven runtime-backed and evidence-backed slices of the target model: enabled policy-allowed local plugins can explicitly run declared setup entries, `entries.commands` can feed the existing JavaScript compatibility runner when a package still provides `main`, declaration-only local `entries.commands` can run as explicit short-lived processes with JSON stdin context, declaration-only local command runs can use short-lived bridge calls for `pet.say` / `pet.action` / `pet.event` / read-only context, declaration-only creator-tools command runs can receive host-owned data/cache/log directories plus bridge-backed action reads / validation / bounded writes, active installed user pack metadata workflows, package-local frame inspection/import, and user-approved picker frame inspection/import, enabled plugins can explicitly open declared HTTP/HTTPS dashboards from Control Center, enabled local plugins can explicitly start/stop declared service entries with logs and runtime state, declared loopback service health endpoints can be checked manually, running services can receive opt-in host-managed periodic health checks, setup/command/service stops now keep stop intent visible until exit confirmation, service stops attempt best-effort process-group cleanup, stubborn services now trigger a bounded force-stop attempt, service cleanup now uses a host-owned process-tree fallback when process-group signalling fails, setup and declaration-only command cleanup now try the same host-owned tree cleanup before direct child kill fallback, plugin cleanup behavior can now be recorded through controlled host evidence artifacts and structured readiness reports, cleanup reports can now be updated through a validation-first CLI, cleanup evidence helper scripts can now generate manual checklists and updater command notes, cleanup evidence archive manifests can now hash report/collector/evidence directories while separating archive validity from cleanup readiness, cleanup evidence runner archives can now execute the conservative collector and preserve transcripts without marking checks passed, packaged-app cleanup evidence runner archives can now launch a built OpenPet app session and map observed setup / command / service cleanup behavior into the existing cleanup report without auto-claiming fallback or global cleanup guarantees, compatible intake summaries can be bridged into community-source submission evidence, and command/setup/service processes avoid shell expansion.
- Phase 75 is now complete: an existing example plugin can now run through a local package -> submission bundle -> maintainer approval rehearsal, while the archive remains workflow evidence rather than external community provenance or release trust.
- Phase 76 is now complete: a public HTTPS archive plugin snapshot can now run through remote-source provenance -> package -> submission bundle -> maintainer approval rehearsal, while the archive remains remote-source workflow evidence rather than proof of independent public ecosystem trust.
- Phase 77 is now complete: macOS release operators can capture or import canonical codesign, notarization, and Gatekeeper evidence files plus summary output, while official macOS readiness still depends on real passing signed evidence.
- Phase 78 is now complete: macOS release workflow runs upload a separate `openpet-macos-release-evidence-<tag>` artifact while keeping those evidence files out of public release downloads.
- Phase 79 is now complete: downloaded macOS workflow evidence artifacts can be archived permanently with `npm run create-macos-release-evidence-archive`, while official release readiness remains gated by release archive and signed closure reports.
- Phase 80 is now complete: declaration-only creator-tools commands can declare `runtime` / `creator-tools` / `hybrid` profile vocabulary, receive host-owned data/cache/log directories, and read / validate / apply bounded action configuration updates through the short-lived bridge without raw filesystem writes.
- Phase 81 is now complete: release-level archive manifests and signed closure reports now require a reviewed `windows-smoke-archive-manifest.json` that matches the archived Windows smoke report path and SHA-256 hash.
- Phase 82 is now complete: declaration-only creator-tools commands can use `assets:inspect` to ask the host to inspect package-local action frame folders through the short-lived bridge, with path traversal and symlink escapes rejected and no sprite generation or raw writes exposed.
- Phase 83 is now complete: declaration-only creator-tools commands can use `assets:generate` to ask the host to import package-local action frame folders and regenerate sprites/action config with frame/pixel limits, while plugin-selected output paths, raw writes, and arbitrary pet-pack writes remain out of scope.
- Phase 84 is now complete: declaration-only creator-tools commands can use `pack-manifest:read` / `pack-manifest:write` for host-mediated active installed pack manifest metadata workflows, while built-in pack edits, arbitrary pack targeting, action-field edits through this route, and general arbitrary pet-pack writes remain out of scope.
- Phase 85 is now complete: declaration-only creator-tools commands can use host-owned native folder picker routes to inspect/import user-approved external frame folders without receiving raw selected paths, raw filesystem grants, plugin-selected output paths, or broad pet-pack writes.
- Phase 86 is now complete: maintainers can create a controlled-host cleanup evidence artifact and create/validate structured plugin cleanup evidence reports, while universal process-tree cleanup guarantees remain out of scope.
- Phase 87 is now complete: maintainers can update cleanup evidence report metadata and individual required checks through a validation-first CLI, while automatic packaged app UI cleanup evidence and universal cleanup guarantees remain out of scope.
- Phase 88 is now complete: maintainers can generate a POSIX cleanup evidence helper that writes manual checklists and updater command notes without marking any cleanup check as passed.
- Phase 89 is now complete: maintainers can generate cleanup evidence archive manifests that hash report, collector, and collected evidence files while keeping archive validity separate from cleanup readiness.
- Phase 90 is now complete: maintainers can run a local cleanup evidence collector chain that archives stdout/stderr/run metadata with `cleanupReady: false` until reviewed evidence is marked pass.
- Phase 91 is now complete: shared TypeScript contracts now cover plugin cleanup archive manifests, collector transcripts, and runner results so Phase 89/90 JSON boundaries type-check against representative fixtures without changing runtime behavior.
- Phase 92 is now complete: shared TypeScript contracts now cover macOS release evidence summaries and macOS evidence artifact archive manifests so Phase 77/79 JSON boundaries type-check against representative fixtures without changing readiness rules.
- Phase 93 is now complete: shared TypeScript contracts now cover Windows smoke evidence summaries and Windows smoke archive manifests so Phase 81 JSON boundaries type-check against representative fixtures without changing readiness rules.
- Phase 94 is now complete: shared TypeScript contracts now cover desktop picker evidence summaries and desktop picker archive manifests so Phase 66/67 JSON boundaries type-check against representative fixtures without changing readiness rules.
- Phase 95 is now complete: shared TypeScript contracts now cover packaged runtime smoke reports and runner evidence payloads so Phase 42/65 JSON boundaries type-check against representative fixtures without changing smoke readiness or picker-link rules.
- Phase 96 is now complete: shared TypeScript contracts now cover desktop picker smoke reports so the source picker report JSON boundary type-checks against representative fixtures without changing smoke readiness rules.
- Phase 97 is now complete: shared TypeScript contracts now cover Windows smoke reports so the source Windows report JSON boundary type-checks against representative fixtures without changing smoke readiness or signing rules.
- Phase 98 is now complete: maintainers can now launch a packaged OpenPet app session, drive plugin cleanup evidence through real setup / declaration-command / service flows, archive the runtime artifact, and keep fallback/force-stop checks pending unless explicit evidence is observed.

## Next Engineering Steps

1. Use `npm run create-macos-release-evidence-archive` to copy the next signed macOS workflow's `openpet-macos-release-evidence-<tag>` artifact into a permanent release archive, then produce real signed release and Windows smoke evidence before changing platform wording.
2. Fill Windows smoke and native picker evidence from real launched or packaged app runs, archive them with their evidence summary/archive manifest commands, then feed both reviewed archive manifests into the release-level manifest and closure report.
3. Continue signed evidence/report hardening, then wait for or source a compatible third-party `plugin.json` package that can pass Phase 104 discovery, Phase 100 intake, Phase 103 bridge, and Phase 99 community-source evidence command.
4. Phase 90 adds a local cleanup evidence runner, Phase 91 adds shared contracts for its manifest/transcript outputs, Phase 92 adds shared contracts for macOS evidence summaries/archive manifests, Phase 93 adds shared contracts for Windows smoke evidence summaries/archive manifests, Phase 94 adds shared contracts for desktop picker evidence summaries/archive manifests, Phase 95 adds shared contracts for packaged runtime smoke reports/evidence payloads, Phase 96 adds shared contracts for desktop picker smoke reports, Phase 97 adds shared contracts for Windows smoke reports, Phase 98 adds packaged-app plugin cleanup evidence automation, and Phase 101 adds shared contracts for the plugin submission evidence chain; future evidence work can add broader real-host archives without changing runtime guarantees.
5. Keep `npm start` functional and keep user-facing configuration in Control Center.
6. Continue Creator Studio from the plugin-owned workflow boundary: plugin owns prompts, generation tasks, run workspaces, QA, and review; OpenPet host owns model secrets, provider calls, output writes, action/pet-pack import, and final trigger-rule persistence.
